<div class="container">
  <% if @artists.size > 0 %>
    <div class="row">
	    <% @artists.each do |artist| %>
	      <div class="col-sm-3">
	        <div class="thumbnail">
	         <%= image_tag(artist.image_url, class:"img-rounded")%>
	           <div class="caption">
	             <h3><%= artist.name %></h3>
	           </div>
	           <div>
	             <%= link_to "Ver más" , '#', class:"btn btn-primary btn-block btn-sm"%>
	           </div>
	         </div>
	        </div>
	        
	    <% end %>
	</div>
  <% end -%>
</div>